import { effect } from "./effect";
import {
  reactive,
  shallowReactive,
  readOnly,
  shallowReadOnly,
} from "./reactive";

export { effect, reactive, shallowReactive, readOnly, shallowReadOnly };
